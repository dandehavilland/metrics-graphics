import 'common/bootstrap_tooltip_popover';

import chart_title from 'common/chart_title';
import register from 'common/register';

var MG = { version: '2.1.0' };

window.MG = MG;

export {
    chart_title,
    register
};

export default MG;
